name: Update Direct Rules
on:
  schedule:
    - cron: '0 4 * * *' 
  workflow_dispatch:      

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
          
      - name: Run Merge Script
        run: python .github/py/merge_direct.py
        
      - name: Commit and Push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # 清理旧路径残留（防止历史文件遗留）
          git rm -f rule/Clash/Direct_Merged.list || true
          # 添加 rule/Merged 目录下的文件
          git add rule/Merged/Direct_Merged.list
          git commit -m "Auto update Direct rules to rule/Merged/" || exit 0
          git push origin main
